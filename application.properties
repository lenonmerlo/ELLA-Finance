spring.application.name=ella

spring.datasource.url=jdbc:postgresql://localhost:5432/ella
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:123456}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

jwt.secret=${JWT_SECRET:HuKj8xZ2n4v6y1A3b9C5d7e8f0g1h2i3j4k5l6m7n8o9p0q1r2s3t4u5v6w7x8y9}
jwt.expiration=${JWT_EXPIRATION:900000}

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

openai.api.key=${OPENAI_API_KEY}
stripe.api.key=${STRIPE_SECRET_KEY}

cors.allowed-origins=${CORS_ALLOWED_ORIGINS}

spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true

# Email
email.enabled=true
email.provider=resend
email.from=ELLA Finanças <onboarding@resend.dev>
email.resend.api-key=${RESEND_API_KEY}

# LGPD
lgpd.contract.version=2025-12-12-v1
lgpd.consent.required=true